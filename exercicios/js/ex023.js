// Inicializa a variável de contagem
let contador = 1;

// Utiliza a estrutura do...while para contar de 1 a 5
do {
    console.log(contador); // Exibe o valor atual do contador no console
    contador++; // Incrementa o valor do contador
} while (contador <= 5);
